# Change Log

## 2.0.0

- 新增自动跟进招聘进度能力。
- 使用 `chrome.alarms` 定时调度，默认每 6 小时检查一次，可在设置中修改。
- 自动从飞书读取仍在进行中的投递记录，并按招聘网站域名分组。
- 新增“安全模式”：只检查已经在 Edge 中打开的招聘网站，复用浏览器现有登录会话。
- 新增“后台标签页实验模式”：必要时创建非激活标签页检查，完成后自动关闭。
- 新增高置信岗位匹配与状态 Diff，只有匹配成功且状态改变时才覆盖飞书当前状态。
- 新增登录失效、等待打开、页面无记录、岗位未匹配、检查失败等诊断状态。
- 新增系统通知：招聘状态发生变化时提醒。
- 新增飞书字段：自动跟进、最后检查时间、状态更新时间、检查状态、登录状态、最近错误。
- Popup 与设置页新增“立即跟进全部”、下次计划时间、上次检查结果。
- 新增 `followup-core.js`，把任务筛选、分组、URL 选择、岗位匹配抽成可独立测试的纯逻辑模块。
- 新增代表性招聘系统测试：飞书招聘、京东自研 SPA、北森/智业、Moka。
- 修复设置页公司名覆盖控件缺失导致的潜在初始化异常。

## 1.6.0

- 增加底层语义提取层，优先读取页面结构化数据、运行时 JSON 和明确字段，再使用可见 DOM 兜底。
- 加强公司名与岗位名识别。
