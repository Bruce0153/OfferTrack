diff --git a/options.css b/options.css
index c4d1a37..1f35689 100644
--- a/options.css
+++ b/options.css
@@ -1,3 +1,5 @@
 *{box-sizing:border-box}body{margin:0;background:#f5f7fb;color:#1f2937;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif}main{max-width:780px;margin:28px auto;padding:0 18px 40px}header{margin-bottom:18px}header h1{margin:0 0 6px;font-size:25px}header p{margin:0;color:#718096}.panel{background:#fff;border-radius:16px;padding:20px;margin:14px 0;box-shadow:0 3px 16px rgba(15,23,42,.05);border:1px solid #eef1f6}.panel h2{font-size:16px;margin:0 0 16px}label{display:block;font-size:13px;font-weight:600;color:#42526b;margin:12px 0}input{display:block;width:100%;margin-top:7px;padding:10px 11px;border:1px solid #dbe1ea;border-radius:9px;outline:none;font:inherit;font-weight:400;background:#fff}input:focus{border-color:#7da7ff;box-shadow:0 0 0 3px rgba(37,99,235,.08)}.inline{display:grid;grid-template-columns:1fr 1fr;gap:12px}.buttons{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}button{border:0;border-radius:9px;padding:9px 13px;background:#e9eef7;color:#344054;cursor:pointer;font-weight:600}.primary{background:#2563eb;color:#fff}.danger{background:#fff1f1;color:#b42318}.big{padding:11px 22px}.hint{font-size:12px;color:#7b8798;line-height:1.65}.status{font-size:13px;min-height:20px;margin-top:12px;line-height:1.6}.check{font-weight:500;display:flex;align-items:flex-start;gap:8px}.check input{width:auto;margin:2px 0 0}.check span{font-size:12px;color:#8792a2}.bottom{display:flex;align-items:center;gap:12px;margin-top:16px}.privacy{font-size:12px;color:#7d8796;line-height:1.7;margin-top:16px;padding:12px 14px;border-left:3px solid #cbd5e1}details summary{cursor:pointer;font-weight:700;color:#334155}@media(max-width:640px){.inline{grid-template-columns:1fr}.buttons{display:grid;grid-template-columns:1fr}.buttons button{width:100%}}
 
 select{display:block;width:100%;margin-top:7px;padding:10px 11px;border:1px solid #dbe1ea;border-radius:9px;outline:none;font:inherit;font-weight:400;background:#fff;color:#344054}select:focus{border-color:#7da7ff;box-shadow:0 0 0 3px rgba(37,99,235,.08)}
+
+.session-health{margin-top:14px;padding:12px 13px;border:1px solid #e7ebf2;border-radius:12px;background:#f8fafc}.session-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.session-head strong{font-size:13px}.session-head span{font-size:12px;color:#64748b}.session-list{margin-top:8px;display:grid;gap:6px}.session-row{display:grid;grid-template-columns:minmax(120px,1fr) auto;gap:8px;align-items:center;padding:7px 8px;background:#fff;border-radius:8px;border:1px solid #eef2f6}.session-row b{display:block;font-size:12px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.session-row small{display:block;font-size:10px;color:#94a3b8;margin-top:2px}.session-tag{font-size:10px;border-radius:999px;padding:4px 7px;background:#eef2f7;color:#526174;white-space:nowrap}.session-tag.ok{background:#ecfdf3;color:#067647}.session-tag.warn{background:#fff7ed;color:#b54708}.session-tag.bad{background:#fef3f2;color:#b42318}.session-actions{margin-top:8px}
diff --git a/options.html b/options.html
index 025f280..55d0c77 100644
--- a/options.html
+++ b/options.html
@@ -49,10 +49,16 @@
       <label class="check"><input id="followUpApiFirst" type="checkbox" /> 优先尝试安全 GET API <span>（仅同源、只读查询；失败自动降级）</span></label>
       <label class="check"><input id="followUpStructuredState" type="checkbox" /> 读取页面 Structured State <span>（JSON / SSR Store；不执行远程代码）</span></label>
       <label class="check"><input id="followUpNotify" type="checkbox" /> 招聘状态发生变化时发送系统通知</label>
+      <label class="check"><input id="followUpNotifySessionIssues" type="checkbox" /> 登录失效、验证码或访问受限首次出现时提醒</label>
       <label class="check"><input id="followUpIncludeTerminal" type="checkbox" /> 继续检查 Offer / 已结束 / 已撤回记录</label>
-      <p class="hint">v2.2 按 API GET → Structured State → Page Scan 三级策略检查。API 只尝试同源、明确属于投递查询且不含提交/撤回/修改等动作的 GET 地址；只有高置信匹配当前飞书岗位时才采用结果。失败会自动降级，不会覆盖原状态。实验后台标签页模式仍只复用 Edge 已有登录会话。</p>
+      <p class="hint">v2.3 在 API GET → Structured State → Page Scan 三级策略之外增加招聘网站会话健康管理。登录失效、验证码和访问受限会进入冷却期，避免每轮重复打开网站；手动“立即跟进”或你已经重新打开该网站时会主动复检并恢复。插件仍不读取或保存 Cookie、手机号、密码。</p>
       <div class="buttons"><button id="runFollowUp" class="primary">立即跟进一次</button><button id="refreshFollowUp">刷新运行状态</button></div>
       <div id="followUpStatus" class="status"></div>
+      <div class="session-health">
+        <div class="session-head"><strong>招聘网站会话</strong><span id="sessionSummary">读取中…</span></div>
+        <div id="sessionList" class="session-list"></div>
+        <div class="buttons session-actions"><button id="refreshSessions">刷新会话状态</button><button id="openSessionProblem">打开需处理网站</button><button id="clearSessions">清理会话记录</button></div>
+      </div>
     </section>
 
 
@@ -77,7 +83,7 @@
     </section>
 
     <div class="bottom"><button id="save" class="primary big">保存设置</button><span id="saveMsg"></span></div>
-    <section class="privacy">隐私：插件不会保存招聘网站账号密码。手动同步、页面自动同步和你主动开启的“自动跟进”会把解析结果/检查状态写入你配置的飞书多维表格；实验后台标签页模式只复用 Edge 当前已有的登录会话。</section>
+    <section class="privacy">隐私：插件不会保存招聘网站账号密码、Cookie 或验证码。会话管理只记录招聘网站域名、健康状态、失败原因和检查时间；自动跟进继续复用 Edge 当前已有的登录会话。</section>
   </main>
   <script src="options.js"></script>
 </body>
diff --git a/options.js b/options.js
index 94494b5..f895f76 100644
--- a/options.js
+++ b/options.js
@@ -15,6 +15,9 @@ document.addEventListener('DOMContentLoaded', async () => {
   $('deleteAlias').onclick = deleteAlias;
   $('runFollowUp').onclick = runFollowUp;
   $('refreshFollowUp').onclick = refreshFollowUpState;
+  $('refreshSessions').onclick = refreshSessionHealth;
+  $('openSessionProblem').onclick = openSessionProblem;
+  $('clearSessions').onclick = clearSessionHealth;
   $('baseUrl').addEventListener('input', () => {
     const current = $('baseUrl').value.trim();
     if (current !== (settings.baseUrl || '')) {
@@ -23,6 +26,7 @@ document.addEventListener('DOMContentLoaded', async () => {
     }
   });
   await refreshFollowUpState();
+  await refreshSessionHealth();
 });
 
 function fill() {
@@ -36,6 +40,7 @@ function fill() {
   $('followUpApiFirst').checked = settings.followUpApiFirst !== false;
   $('followUpStructuredState').checked = settings.followUpStructuredState !== false;
   $('followUpNotify').checked = settings.followUpNotify !== false;
+  $('followUpNotifySessionIssues').checked = settings.followUpNotifySessionIssues !== false;
   $('followUpIncludeTerminal').checked = !!settings.followUpIncludeTerminal;
 }
 
@@ -57,6 +62,7 @@ function collect() {
     followUpStructuredState: $('followUpStructuredState').checked,
     followUpApiTimeoutSeconds: Number(settings.followUpApiTimeoutSeconds || 6),
     followUpNotify: $('followUpNotify').checked,
+    followUpNotifySessionIssues: $('followUpNotifySessionIssues').checked,
     followUpIncludeTerminal: $('followUpIncludeTerminal').checked,
     followUpTabTimeoutSeconds: Number(settings.followUpTabTimeoutSeconds || 25),
     customSites: settings.customSites || {},
@@ -223,3 +229,70 @@ function setFollowUpStatus(text, error=false) {
   $('followUpStatus').textContent = text || '';
   $('followUpStatus').style.color = error ? '#c94646' : '#647085';
 }
+
+
+async function refreshSessionHealth() {
+  const summaryEl = $('sessionSummary');
+  const listEl = $('sessionList');
+  summaryEl.textContent = '读取中…';
+  try {
+    const res = await chrome.runtime.sendMessage({ type: 'GET_SESSION_HEALTH' });
+    if (!res?.ok) throw new Error(res?.error || '无法读取会话状态');
+    const q = res.summary || {};
+    summaryEl.textContent = `健康 ${q.healthy || 0} · 需登录 ${q.loginRequired || 0} · 验证/受限 ${(q.challenge || 0) + (q.rateLimited || 0)} · 异常 ${q.error || 0}`;
+    renderSessionList(res.entries || []);
+  } catch (e) {
+    summaryEl.textContent = '读取失败';
+    listEl.innerHTML = `<div class="hint">${escapeHtml2(e?.message || String(e))}</div>`;
+  }
+}
+
+function renderSessionList(entries) {
+  const root = $('sessionList');
+  root.innerHTML = '';
+  const visible = (entries || []).slice(0, 8);
+  if (!visible.length) {
+    root.innerHTML = '<div class="hint">尚无会话检查记录。运行一次自动跟进后会在这里显示。</div>';
+    return;
+  }
+  for (const e of visible) {
+    const row = document.createElement('div');
+    row.className = 'session-row';
+    const bad = e.state === 'login_required' || e.state === 'error';
+    const warn = e.state === 'challenge' || e.state === 'rate_limited';
+    const cls = e.state === 'healthy' ? 'ok' : bad ? 'bad' : warn ? 'warn' : '';
+    const sub = [e.providerName, e.reason, e.lastCheckedAt ? fmtTime(e.lastCheckedAt) : ''].filter(Boolean).join(' · ');
+    row.innerHTML = `<div><b>${escapeHtml2(e.host || '')}</b><small>${escapeHtml2(sub)}</small></div><span class="session-tag ${cls}">${escapeHtml2(e.label || e.state || '未知')}</span>`;
+    root.appendChild(row);
+  }
+}
+
+async function openSessionProblem() {
+  try {
+    const res = await chrome.runtime.sendMessage({ type: 'GET_SESSION_HEALTH' });
+    const entry = (res?.entries || []).find(e => ['login_required','challenge','rate_limited'].includes(e.state));
+    if (!entry) return setFollowUpStatus('当前没有需要重新登录或验证的网站。');
+    const url = entry.lastUrl || `https://${entry.host}/`;
+    await chrome.tabs.create({ url, active: true });
+    setFollowUpStatus(`已打开 ${entry.host}。完成登录/验证后，点击“立即跟进一次”即可立即恢复检查。`);
+  } catch (e) {
+    setFollowUpStatus(e?.message || String(e), true);
+  }
+}
+
+async function clearSessionHealth() {
+  try {
+    const res = await chrome.runtime.sendMessage({ type: 'CLEAR_SESSION_HEALTH' });
+    if (!res?.ok) throw new Error(res?.error || '清理失败');
+    renderSessionList(res.entries || []);
+    const q = res.summary || {};
+    $('sessionSummary').textContent = `健康 ${q.healthy || 0} · 需登录 ${q.loginRequired || 0} · 验证/受限 ${(q.challenge || 0) + (q.rateLimited || 0)} · 异常 ${q.error || 0}`;
+    setFollowUpStatus('会话健康记录已清理；不会清除浏览器登录状态。');
+  } catch (e) {
+    setFollowUpStatus(e?.message || String(e), true);
+  }
+}
+
+function escapeHtml2(v) {
+  return String(v || '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
+}
diff --git a/popup.css b/popup.css
index 41a8269..cf75d21 100644
--- a/popup.css
+++ b/popup.css
@@ -1,2 +1,4 @@
 *{box-sizing:border-box}body{margin:0;width:380px;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif;background:#f6f8fb;color:#1f2937}main{padding:14px}header{background:white;border-radius:14px;padding:12px 14px;box-shadow:0 2px 10px rgba(0,0,0,.05)}header div{display:flex;align-items:center;justify-content:space-between}header strong{font-size:16px}header span{font-size:12px;color:#64748b}.hero{text-align:center;padding:15px 0 10px}.hero span{font-size:38px;font-weight:700}.hero small{display:block;color:#7a8496;margin-top:2px}.preview{background:#fff;border-radius:12px;padding:6px 10px;margin-bottom:10px;max-height:190px;overflow:auto;border:1px solid #eef1f5}.preview-row{display:flex;gap:8px;align-items:center;justify-content:space-between;padding:8px 2px;border-bottom:1px solid #f1f3f6}.preview-row:last-child{border-bottom:0}.preview-row div{min-width:0}.preview-row b,.preview-row span,.preview-row small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.preview-row b{font-size:12px}.preview-row span{font-size:12px;color:#6b7280;margin-top:2px}.preview-row small{font-size:10px;color:#98a2b3;margin-top:2px}.preview-row em{font-size:11px;font-style:normal;background:#f1f5f9;border-radius:999px;padding:4px 7px;white-space:nowrap}.more,.empty{text-align:center;font-size:11px;color:#94a3b8;padding:7px}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:10px}.stats div{background:#fff;border-radius:12px;text-align:center;padding:9px}.stats b{display:block;font-size:18px}.stats span{font-size:11px;color:#7a8496}.message{min-height:34px;font-size:12px;line-height:1.45;padding:2px 3px 8px}.primary,.secondary button{border:0;border-radius:10px;cursor:pointer}.primary{width:100%;padding:11px;background:#2563eb;color:#fff;font-weight:700}.primary:disabled{opacity:.6}.secondary{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}.secondary button{padding:9px;background:#e9eef7;color:#344054}footer{font-size:11px;color:#8a94a6;margin-top:10px;text-align:center}
 .followup-card{background:#fff;border:1px solid #eef1f5;border-radius:12px;padding:10px 11px;margin:0 0 10px}.followup-card>div{display:flex;align-items:center;justify-content:space-between;gap:8px}.followup-card strong{font-size:12px}.followup-card span{font-size:11px;color:#64748b}.followup-card small{display:block;color:#94a3b8;font-size:10px;line-height:1.45;margin-top:4px}.followup-card button{width:100%;border:0;border-radius:9px;background:#eef4ff;color:#245edb;font-weight:700;padding:8px;margin-top:8px;cursor:pointer}.followup-card button:disabled{opacity:.6;cursor:default}
+
+.session-meta{margin-top:3px!important;color:#64748b!important}.session-meta.problem{color:#b42318!important}
diff --git a/popup.html b/popup.html
index 1315fb6..6e3951d 100644
--- a/popup.html
+++ b/popup.html
@@ -21,6 +21,7 @@
     <section class="followup-card">
       <div><strong>自动跟进</strong><span id="followUpState">读取中…</span></div>
       <small id="followUpMeta">默认每 6 小时检查一次进行中的投递</small>
+      <small id="sessionMeta" class="session-meta">会话状态读取中…</small>
       <button id="followUpNow">立即跟进全部</button>
     </section>
     <button id="sync" class="primary">同步到飞书</button>
diff --git a/popup.js b/popup.js
index 139b671..65f6294 100644
--- a/popup.js
+++ b/popup.js
@@ -11,6 +11,7 @@ document.addEventListener('DOMContentLoaded', async () => {
   $('followUpNow').onclick = runFollowUpNow;
   await loadState();
   await loadFollowUpState();
+  await loadSessionHealth();
   await scan();
 });
 
@@ -148,6 +149,7 @@ async function runFollowUpNow() {
     setMessage('自动跟进任务已启动，可关闭插件窗口；任务会在后台继续运行。');
     await new Promise(resolve => setTimeout(resolve, 700));
     await loadFollowUpState();
+    await loadSessionHealth();
   } catch (e) {
     setMessage(e?.message || String(e), true);
   } finally {
@@ -156,3 +158,22 @@ async function runFollowUpNow() {
   }
 }
 
+
+
+async function loadSessionHealth() {
+  const el = $('sessionMeta');
+  if (!el) return;
+  try {
+    const res = await chrome.runtime.sendMessage({ type: 'GET_SESSION_HEALTH' });
+    if (!res?.ok) throw new Error(res?.error || '读取失败');
+    const q = res.summary || {};
+    const problems = (q.loginRequired || 0) + (q.challenge || 0) + (q.rateLimited || 0) + (q.error || 0);
+    el.textContent = problems
+      ? `会话：${q.healthy || 0} 健康 · ${q.loginRequired || 0} 需登录 · ${(q.challenge || 0) + (q.rateLimited || 0)} 验证/受限 · ${q.error || 0} 异常`
+      : `会话：${q.healthy || 0} 个网站健康`;
+    el.classList.toggle('problem', problems > 0);
+  } catch {
+    el.textContent = '会话状态暂不可用';
+    el.classList.remove('problem');
+  }
+}
